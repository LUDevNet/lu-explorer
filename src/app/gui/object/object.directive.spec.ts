import { ObjectDirective } from "./object.directive";

describe("ObjectDirective", () => {
  it("should create an instance", () => {
    //const directive = new ObjectDirective();
    //expect(directive).toBeTruthy();
  });
});
